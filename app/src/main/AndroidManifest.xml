<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CivicWatch"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- File Provider for sharing images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Splash Activity - Launcher -->
        <activity
            android:name=".presentation.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Image Gallery Activity - Using AppCompat theme -->
        <activity
            android:name=".presentation.activities.ImageGalleryActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicWatch" />

        <!-- Fullscreen Image Activity - Using NoActionBar theme -->
        <activity
            android:name=".presentation.activities.FullscreenImageActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Login Activity -->
        <activity
            android:name=".presentation.activities.LogInActivity"
            android:exported="false" />
        <!-- Language Settings Activity -->
        <activity
            android:name=".presentation.activities.LanguageSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.CivicWatch" />

        <!-- Main Activity -->
        <activity
            android:name=".presentation.activities.MainActivity"
            android:exported="false" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC-_OcZ31INflia9epeX3fWoeBqBOIB3J8" />

    </application>

</manifest>